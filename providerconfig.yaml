apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: aws-provider
  namespace: crossplane-system
spec:
  region: us-east-1                 # change if your AWS resources are in another region
  credentials:
    source: IRSA                     # use IAM Role attached to ServiceAccount
